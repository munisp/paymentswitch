[package]
name = "adaptive-ratelimiter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
panic = "abort"
