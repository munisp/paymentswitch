apiVersion: apps/v1
kind: Deployment
metadata:
  name: p2p-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: p2p-service
  template:
    metadata:
      labels:
        app: p2p-service
    spec:
      containers:
      - name: p2p-service
        image: nextgen-payment-switch/p2p-service:latest
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        ports:
        - containerPort: 8000
