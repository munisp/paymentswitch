apiVersion: apps/v1
kind: Deployment
metadata:
  name: corporate-onboarding-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: corporate-onboarding-service
  template:
    metadata:
      labels:
        app: corporate-onboarding-service
    spec:
      containers:
      - name: corporate-onboarding-service
        image: nextgen-payment-switch/corporate-onboarding-service:latest
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        ports:
        - containerPort: 8000
