events {}
http {
  server {
    listen 3000;
    location = /api/health {
      default_type application/json;
      return 200 '{"status":"healthy","service":"validation-backend"}';
    }
    location / {
      default_type application/json;
      return 200 '{"status":"ok","service":"validation-backend"}';
    }
  }
}
